# This is a basic workflow to help you get started with Actions

name: CI

env: 
  PROJECT_NAME:    PointCloudSegmentation
  BUILD_DIR:       docker-env
  DOCKER_REP_NAME: point_cloud_segmentation
  DOCKER_HUB_NAME: dockeridze
on:
  fork:
  push:
    branches: [ dev, master ]
  pull_request:
    branches: [ master ]

jobs:
  codeStyle:
    runs-on: [ubuntu-latest]
    steps: 
      - uses: actions/checkout@v2
        name: Actions git repo
      - uses: peter-evans/autopep8@v1
        name: Autopep8
        with:
          args: --recursive --in-place --aggressive --aggressive .
  build:
    runs-on: ubuntu-latest
    needs: codeStyle
    steps:
      - uses: actions/checkout@v2
        name: Actions git repo
      - name: Docker login
        run: |
          docker login -u ${{ secrets.DOCKER_HUB_LOGIN }} -p ${{ secrets.DOCKER_HUB_PASSWORD }} docker.io
      - name: Generate build number
        uses: einaregilsson/build-number@v2 
        with:
          token: ${{secrets.GIT_TOKEN}}        
      - name: Print new build number
        run: echo "Build number is $BUILD_NUMBER"
      - name: Docker build
        run: |
          ls
          docker build -t ${{ secrets.DOCKER_HUB_LOGIN }} $BUILD_DIR/
          docker tag ${{ secrets.DOCKER_HUB_LOGIN }} ${{ secrets.DOCKER_HUB_LOGIN }}/$DOCKER_REP_NAME:ver_$BUILD_NUMBER
          docker images
      - name: Docker push
        run: |
          docker push ${{ secrets.DOCKER_HUB_LOGIN }}/$DOCKER_REP_NAME:ver_$BUILD_NUMBER
      - name: Send telegram notification
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          args: The PointCloudSegmentation build № $BUILD_NUMBER builded.
    # Runs a single command using the runners shell
    # - name: clone repo
    #  run: git clone https://github.com/iamilyasedunov/PointCloudSegmentation.git -b dev

    # Runs a set of commands using the runners shell
    # - name: build image
    #  run: |
    #      cd PointCloudSegmentation/docker-env
    #      docker build -t jupyter .
    #      docker images
    # Push image to docker-hub
    # - name: build image
    #  run: docker push dockeridze/point_cloud_segmentation:initial-build
